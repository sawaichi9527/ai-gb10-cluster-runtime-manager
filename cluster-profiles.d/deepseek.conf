#!/usr/bin/env bash
# =====================================================================
# deepseek cluster profile — PLACEHOLDER (NOT deployed).
#
# Target (documentation only; not validated until image + model land):
#   NVIDIA DeepSeek-V4-Flash-0731 NVFP4 on a derived AEON image
#   lineage: 2026-08-24-v0.27.1-omni -> 2026-09-04-v0.27.1-omni-ds4flash0731-r1
#   TP2 (rank0 API :1234), DSpark off for r1 control, FP8 KV baseline,
#   PIECEWISE baseline, start 64K context.
#
# IMPORTANT: do NOT populate IMAGE / BODY_REL / DRAF_REL (fake paths)
# until the derived image and model dirs are actually present and
# verified on BOTH nodes. PLACEHOLDER=true makes every op fail safely
# with "not deployed yet" and never start containers.
# =====================================================================
PROFILE_ID="deepseek"
DISPLAY_NAME="DeepSeek-V4-Flash-0731 (placeholder)"
PLACEHOLDER="true"

# Intentionally empty — failing safely is the correct behavior here.
IMAGE=""
BODY_REL=""
DRAF_REL=""
